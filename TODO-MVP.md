# TODO MVP

Что осталось сделать/проверить до релиза, кроме биллинга/Premium-инфраструктуры
(биллинг отслеживается отдельно в `PLAY_CONSOLE_SETUP_TODO.md`).

## Блокеры публикации

- [x] **1. Шифрование данных на устройстве** (ТЗ 1.4.5, раздел 3). Room переведён на
      SQLCipher, ключ — 256-бит, генерируется один раз и хранится в
      EncryptedSharedPreferences (Keystore). Проверено на устройстве: файл БД больше
      не открывается как обычный SQLite, ключ переживает перезапуск процесса.

- [x] **2. Политика конфиденциальности.** Текст составлен (`PRIVACY_POLICY.md`),
      опубликован на Google Sites, ссылка добавлена в Settings (открывается во
      внешнем браузере). Осталось вставить тот же URL в Google Play Console →
      App content → Privacy Policy — это на стороне пользователя, доступа туда нет.

## Важно — доделать функциональность

- [x] **3. Главный экран**: реальная сводка за неделю (кол-во приступов + средняя
      интенсивность за 7 дней), считается из БД через `HomeViewModel`. Проверено
      вживую на эмуляторе — пустое состояние и состояние с данными.

- [x] **4. Премиум-аналитика — сама логика расчётов.** Реализовано: корреляция
      триггеров (% тяжёлых приступов с этим триггером), эффективность медикаментов
      (средняя интенсивность — честно помечено как корреляция, не клиническое
      измерение), тренды по дню недели/времени суток/сезону. Показывается вместо
      тизеров, когда `premiumStatus = true`. Проверено вживую на нескольких записях.
      Корреляция с циклом сознательно не сделана — нет самого модуля отслеживания
      цикла, это отдельная фича (новая сущность данных + экран логирования).

- [ ] **5. Погодная корреляция без барометра.** Сейчас работает только через
      сенсор устройства (`PressureSensorReader`). Нужен fallback на
      OpenWeatherMap API (ТЗ раздел 6) для телефонов без барометра — иначе
      бесплатный дифференциатор недоступен части пользователей.

- [ ] **6. Напоминания (ТЗ 2.7).** В `UserSettings` есть флаг `remindersEnabled`,
      но нет ни переключателя в Settings, ни реального планирования уведомлений
      (WorkManager/AlarmManager). Помечено как «не критично для MVP» в ТЗ —
      можно осознанно отложить.

- [x] **9. Системная кнопка «назад» теряла данные без сохранения.** Добавлен
      `BackHandler` на `LogDetailsScreen` и `AttackRecordScreen` (у обеих был тот же
      баг) — теперь back сохраняет перед выходом, как Done/Save. Диалог удаления
      по-прежнему просто закрывается по back, не трогая запись (AlertDialog
      перехватывает back раньше, чем экран). Проверено вживую на эмуляторе:
      выбранный симптом/триггер теперь переживает системный back.

## Проверить руками

- [x] **7. PDF-отчёт.** Сгенерирован вживую на 43 записях (через debug seeder,
      см. ниже) — заголовок, период, итоги, график по месяцам, полный список
      приступов с локализацией/симптомами/триггерами/медикаментами — всё корректно,
      share-диалог открывается. Заодно этим же прогоном нашли и починили баг в
      `SimpleBarChart` (показывал только один столбец при нескольких точках данных —
      незаметно раньше, пока не было достаточно разнородных данных).

- [x] **8. Тест на 2+ устройствах разных производителей** (ТЗ раздел 8 DoD).
      Устройство #1 — Realme RMX2111, Android 12, 1080×2400. Устройство #2 —
      Motorola Edge 60 Pro, Android 16, 1220×2712. На обоих по Wi-Fi debugging
      проверено: онбординг, быстрый лог, тёмная тема, шифрованная БД (Keystore
      на разных чипсетах), ссылка на Privacy Policy — рендер и функциональность
      корректны на обоих разрешениях/производителях, крашей в logcat нет.
      Попутно нашли реальный баг — см. ниже.

---

**Инструмент для тестирования:** в debug-сборке в Settings внизу есть панель
«Debug tools» — «Seed 6 months of test data» заполняет БД ~100 случайными приступами
за 6 месяцев (для наглядной проверки PDF/Insights и фильтров по периоду), «Clear test
data» убирает только эти записи. В release-сборку не попадает вообще (отдельный
source set, не runtime-проверка) — проверено на dex release-APK.

**UI Insights:** секции обёрнуты в карточки для визуального разделения (было —
голая заливка с отступами, все блоки сливались).
